# GAPS — Sentinel

> 🌐 [Русский](GAPS.md) (основная версия) · **English**

Tracking of open questions, items to verify, and known risks. Format: `GAP-[CAT]-[NUM]`.
Categories: ARCH (architecture), VERIFY (needs fact-check), RISK, AGENT (missing tooling), DECISION (awaiting user).

> Source: design workflow synthesis (2026-06-23) + BUILD-ONLY constraint reconciliation.

---

## Decision / constraint gaps

| ID | Priority | Gap | Status |
|----|----------|-----|--------|
| GAP-DECISION-001 | P1 | **BUILD-ONLY interpretation.** OSS libraries (Playwright, LangGraph, Anthropic SDK, MCP SDK) = allowed ("writing"); turnkey servers/SaaS products = not allowed. | **RESOLVED 2026-06-23** (confirmed by user). GAP-ARCH-002 unblocked → closed. |
| GAP-ARCH-001 | P1 | **`pw-executor` is now critical-path.** Building + maintaining our own TS Playwright execution server is the largest single component and the highest ongoing-maintenance risk (tracks Playwright lib API churn). Mitigation: thin tool layer over Playwright's stable `Locator`/`accessibility` API; contract tests asserting tool names+schemas; pin Playwright version. | OPEN |
| GAP-ARCH-002 | P2 | If GAP-DECISION-001 resolves to "no OSS either", re-evaluate the entire stack (LangGraph → bespoke loop, Playwright → raw CDP). Would invalidate ADR-002/004/005. | BLOCKED on GAP-DECISION-001 |

## VERIFY (fact-checks — anti-hallucination; do not assume)

| ID | Priority | Item | Resolve by |
|----|----------|------|------------|
| GAP-VERIFY-001 | P1 | Underlying **Playwright library API** capabilities at the pinned version: `accessibility` snapshot surface, `tracing.start/stop`, locator engines (role/text/label/testid), `screenshot`. We DEFINE our own tool surface in `pw-executor`, but it sits on these primitives. | M0 |
| GAP-VERIFY-002 | P1 | **Python↔MCP binding** package + maturity (e.g. `langchain-mcp-adapters`): stdio transport stability, tool-schema validation strictness, subprocess error propagation. Both ends are ours now (lowers risk). Keep a thin swappable adapter so a ~300-line custom JSON-RPC client is a low-risk fallback. | M1 |
| GAP-VERIFY-003 | P2 | **LangGraph `SqliteSaver` / `AsyncPostgresSaver`** checkpointer API + separate-DB-file usage; `interrupt`/pause semantics for the human gate. | M1 |
| GAP-VERIFY-004 | P3 | **Anthropic SDK** structured-output / tool-use call shape for plan + heal nodes (model IDs `claude-opus-4-8`, `claude-sonnet-4-6`). | M1 · **✅ RESOLVED 2026-07-05 (ADR-057)** — plan/heal use Anthropic forced `tool_use` (`complete_json`, `brain/llm.py`) + OpenAI `json_schema`; offline-verified + adversarial-review; live-provider smoke → GAP-VERIFY-005/M9-LIVE. |
| GAP-VERIFY-005 | P2 | **Real-provider smoke for the provider-agnostic backend (M6/ADR-019).** The environment blocks network → offline is covered by `FakeBackend`; real OpenAI-compat behaviour (at least one router: OpenRouter/DeepSeek/Qwen/Gemini-compat) is **user-run**: is `temperature=0` accepted?; `max_tokens` vs `max_completion_tokens` (o-series); missing `usage` (Ollama/vLLM); vision `image_url` data-URI. Instructions in `docs/M6_CONTRACT.md`. **LiteLLM router documented (ADR-045):** `docs/ADAPTERS.md` + the `litellm` compose profile — a concrete OpenAI-compat router for this smoke. | M6 (user-run) |
| GAP-VERIFY-006 | P2 | **MCP `sampling/createMessage` support across hosts** for the M7 `SamplingBackend` (ADR-020): M7 implemented, offline-verified; the server `create_message` API is confirmed on the installed `mcp`. Remaining is **user-run** — a real host (Claude Desktop — yes; **OpenCode/Kilocode — confirm capability before production use**). No sampling → the backend is unavailable → fallback to heuristic/L1–L6. **MCP-Inspector documented (ADR-045, `docs/ADAPTERS.md` §3)** as an stdio host: `tools/list` (4 tools) + live `sampling/createMessage` — a concrete verification path; a live run across hosts remains user-run. | M7 (user-run) |

## Design open questions (from synthesis)

| ID | Priority | Question |
|----|----------|----------|
| GAP-ARCH-003 | P1 | Precise **coverage metric** for ADR-010: "interactive element exercised" counted per `semantic_id` / per `(page,role,name)` / per distinct flow? Must not reward trivial clicks nor punish small apps. |
| GAP-ARCH-004 | P1 | How to reliably derive the scenario's target **SUBTREE** for `dom_subtree_hash` scoping (nearest landmark/role container?) without over/under-scoping. Validate against real DOM drift. |
| GAP-ARCH-005 | P2 | **Calibration bootstrap volume:** how many human-verified outcomes before lowering auto-accept threshold from the 0.90 default, and over what window? |
| GAP-ARCH-006 | P2 | Is explore-mode **soft verification on Sonnet** worth its cost, or does a deterministic post-action state check (URL change, expected element present) suffice? Measure @ M3. |
| GAP-ARCH-007 | P2 | **Cross-browser scope:** Chromium-only @ MVP assumed; confirm Firefox/WebKit near-term need (affects golden-baseline portability — hashes differ per engine). **Resolved (2026-06-28): Chromium-only by design (ADR-036)** — Firefox/WebKit deferred (GAP-OPS-001). |
| GAP-ARCH-008 | P1 | **Auth/secret handling** for the AUT (storage-state/cookie injection): where do credentials live (home-lab Vault?) and how are they referenced in `RunConfig` without landing in traces/transcripts? |

> **Re-tag (Foundation cycle, 2026-06-27):** GAP-ARCH-003..006 move to the **live-verify** phase (tracked under **M9-LIVE** — measured on a live run, not offline). GAP-ARCH-007 (browser coverage) is folded into **GAP-OPS-001** (pw-executor lifecycle). GAP-ARCH-008 (auth/secrets) is partially closed by M9.1 (`secretRef`+`storageState`); the remainder (Vault/Helm Secret) → **GAP-SEC-001** / M11.3.

## M9 — capability gaps (design session 2026-06-26 → docs/M9_CONTRACT.md)

| ID | Priority | Gap | Target |
|----|----------|-----|--------|
| GAP-M9-01 | P1 | No `fill`/`type`/`press`/`select` in pw-executor → no forms/search/**login** | M9.1 (blocker #1) — **DONE offline 2026-06-26 (ADR-026):** 6 tools (+`expect`/`saveStorageState`) in both transports; tsc+FakeEx green. Live UI — on "go" |
| GAP-M9-02 | P1 | `GoalPlanner` (NL→plan, explore-first grounding) + `--mode explore\|goal\|describe` + auto-default | M9.2a (goal-mode + index-grounding, ADR-027) + M9.2b (two-phase explore-then-scenario §L + describe-first §B + one-shot `build_scenario`/`reconcile`, ADR-028) **DONE offline**. Live goal/describe run — on "go" |
| GAP-M9-03 | P2 | Non-MCP HTTP/gRPC control API + OSS chat-UI in DH (plus the MCP path via M7) | M9.3: control-API (`cmd/control-api`, ADR-032) + setup-WebUI live-mode **DONE** (Wave B p1/p2); OSS chat-UI (`docs/chat/index.html`) + CI templates (`docs/ci-templates/`) — **DONE (M9.3-tail, ADR-040)**. M9.3 complete |
| GAP-M9-04 | P2 | Auth adapter (storageState + Keycloak/OIDC), creds from Vault; **login as a test target** | M9.1 (storageState load/save + login-as-test + `secretRef`) **DONE offline**; pluggable auth-adapter + Vault/Helm Secret — M9.7 |
| GAP-M9-05 | P2 | In-app tabs (perception `role=tab`/`tabpanel`) + browser multi-tab/context (multi-page in pw-executor) | M9.4 **DONE offline (Wave A)**: `[role=tab]` in interactives/setOfMarks (A5) + multi-page (`browser.tabs`/`switchTab`, `context.on('page')`, A6). Live multi-tab + brain "plan references a tab" — on "go" |
| GAP-M9-06 | P2 | Inject `traceparent` into all browser requests → correlate a UI action with the backend/Kafka trace in Tempo | M9.5 **DONE offline (Wave A)**: `context.route('**/*')` injects `traceparent` (gated on OTLP, no-op without a collector). Live backend correlation — on "go" (needs a collector + OTel AUT) |
| GAP-M9-07 | P2 | Browser modes: headed + CDP-attach to the user's browser (`connectOverCDP`) + co-pilot takeover/return | **M9.6 DONE offline (Wave D, ADR-037):** headed (`PW_HEADLESS=0`) + CDP-attach (`PW_CDP_ENDPOINT` — reuses the user's context, teardown doesn't close their browser) in `pw-executor` (`launch.ts`+`server.ts`), env-toggle with no new RPCs; co-pilot takeover/return → **M9.8**; live-verify (visible window / real CDP) pending «go». Contract `M9.6_CONTRACT.md` |
| GAP-M9-08 | P3 | Pluggable adapters (auth/deploy/model/backend) — universality beyond DH | **model/backend adapter addressed (ADR-045):** LiteLLM optional router behind `LLM_BASE_URL` + the umbrella `docs/ADAPTERS.md`. Remainder split (ADR-056 §2): **open-core auth/deploy adapter SPI + a basic adapter → M9.7**; **enterprise-auth (Keycloak/OIDC/Vault/SSO/RBAC) → `[M-COMMERCIAL-auth]`** (🔒 private `sentinel-enterprise`, NOT Apache) |
| GAP-M9-09 | P2 | RunConfig file (YAML) + config surfaces: mode/goal/auth/budgets via flags · env · file · interactive (chat). Today flags+env only, per-run | M9.2a (minimal RunConfig: mode/goal/planner/budgets) + M9.2b (rich: declarative `auth:`→STORAGE_STATE*/PW_NO_TRACE + named `scenarios:` + `--scenario` selector, ADR-028) **DONE offline**; per-role model endpoints — M9.7; chat surface — M9.3 |
| GAP-M9-10 | P2 | Validation / negative testing: an invalid-input generator per field type/mask + an assertion layer ("UI rejected the input") | M9.1 (assert primitive `browser.expect` + `expect_ok` polarity + `brain/validation.py` **sketch**) **PARTIAL — DONE offline**; full generator (masks/bounds/schema) — M9.2 |
| GAP-M9-11 | P3 | **Security module (M10, separate):** XSS/CSRF/IDOR/auth-bypass/sensitive-data-in-DOM on top of the explore map; **authorization-gated** | M10 |
| GAP-M9-12 | P3 | CI templates: a Jenkinsfile + `.gitlab-ci.yml` (Sentinel = a CLI + exit codes → any CI on commit) | M9.3 — **DONE (M9.3-tail)**: `docs/ci-templates/{Jenkinsfile,.gitlab-ci.yml,README+en}`, exit 0/1/2/3 mapping |
| GAP-M9-13 | P2 | **Action recorder (M9.8 extension):** content-script capture of click/input → events → grounding into steps (reuse M9.2b `reconcile`). Absent today (describe-mode is NL→scenario, not event-capture). Blocks M9.8 implementation | M9.8 (ADR-038). **Transport ready (M9.8-prep, WS `/v1/stream`, ADR-043).** **✅ DONE:** `extension/` (#42-46) — content-script recorder with mandatory redaction + `brain/record_bridge.py` (record→scenario) + recorder-e2e. Dev-only; live replay = M9-LIVE |
| GAP-M9-14 | P2 | **Streaming transport extension↔brain (M9.8):** the control-API (M9.3) was request/response only (no WS/SSE). A streaming endpoint (or native-messaging host) is needed. Blocks M9.8 | **DONE:** server→client **SSE** `GET /v1/runs/{id}/events` (ADR-040) + client→server **hand-rolled WS** `GET /v1/stream` (M9.8-prep, ADR-043 — recorder NDJSON ingest → `events.ndjson`, token via `Sec-WebSocket-Protocol`, 6 httptest race). reuses ADR-032 security. native-messaging — the air-gapped alternative (not implemented) |
| GAP-M9-15 | P2 | **Takeover/return protocol (M9.8):** handing control agent↔human on one CDP session (on top of M9.6). Undefined. Blocks co-pilot | M9.8 (ADR-039). **Transport (WS `/v1/stream`, ADR-043) + front skeleton (ADR-044) ready.** **MV3 takeover/return ✅ DONE** (#47, `extension/src/background/takeover.ts` — `chrome.debugger` attach/return, visible banner, auto-detach, reconcile); **brain interrupt/resume ✅ DONE** (R3/ADR-054, GAP-M9-18) |
| GAP-M9-16 | P2 | **Replay/baseline INSIDE the UI (M9.9/R1, ADR-046):** run/re-run a ready `scenario.json`/`plan.json` + set baseline from the chat/setup UI, not only CLI/CI. Deferred in M9.3 as a spawn surface; now **un-deferred** | **M9.9 [me]** — control-API `mode=replay\|baseline` + `from_run:<run_id>` (whitelist+traversal-guard, never an arbitrary path) + ▶/🔁/📌 in vanilla-UI + verdict. Plumbing offline; live replay = M9-LIVE. **✅ DONE: R1a backend (ADR-047: control-API + 12 httptests) + R1b UI (▶/🔁/📌 + verdict in `#build`/`#chat`/`chat/`/`setup/`).** |
| GAP-M9-17 | P2 | **Multi-turn conversational authoring (M9.10/R2, ADR-046):** everything is one-shot today (1 message = 1 run); no cross-turn context, no mid-run correction. Was an unnumbered "brain-extension milestone" | **M9.10 [me]** — brain `chat` `RUN_MODE` on the existing LangGraph checkpointer (stable `conversation_id`→`thread_id`) + a `messages` add_messages channel + a multi-turn panel. **✅ CLOSED — R2a backend (ADR-048): conversation_id→thread_id resume, conditional-entry refine, shared `state/conversations.db`, agentctl/control-API `conversation_id`, offline two-turn test + R2b UI: vanilla `docs/chat`+`docs/index#chat` mint `conversation_id`/conversation, cumulative transcript, 🆕 "New conversation", one-shot disclaimers rewritten. Live=M9-LIVE.** |
| GAP-M9-18 | P2 | **F4 takeover execution-side (brain) (M9.8/R3, ADR-046):** the brain can't pause a run on a takeover signal and resume on return with a state delta ("no live switching mid-run"). Pairs with extension #47 | **✅ DONE (R3, ADR-054, 2026-06-30)** — RunControl `Control.takeover` + `Takeover`/`Return` RPCs; brain `checkpoint` arm → dedicated `takeover` node `interrupt()`/`Command(resume)` (latch `_takeover_armed`); `_resume_through_takeovers`; control-API `/v1/stream` forwards `{type:control,action:takeover\|return}` → orchestrator. Offline fake-client test (`tests/test_r3_takeover_offline.py` + Go). Live e2e (extension #47 + CDP) = M9-LIVE. Closes the brain half of GAP-M9-15 |
| GAP-M9-19 | P3 | **Stale site-map in refine (M9.10 follow-up):** a warm refine turn authors over the `site_map` as it was at cold turn-1; AUT drift between turns is invisible → it can author against a stale map. | **M13 wave 6 ✅ (partial)** — opt-in `SENTINEL_REFINE_REVERIFY=1` forces a cold re-explore on a warm thread (`brain/__main__.py`), refreshing a stale `site_map`. Auto-detection (current-page a11y hash vs persisted on the warm turn) needs a browser — which a warm turn has none of by design → **M9-LIVE**. Until then "New conversation" / reverify = a fresh explore. |
| GAP-M9-20 | P3 | **Unbounded conversation history (M9.10 follow-up):** the R2a `messages` channel + `_user_turns` pass ALL user turns; `conversations.db` accumulates everything → the prompt/checkpoint bloats on long conversations (per-LLM-call is bounded by `[:8000]`/`max_tokens`, but history is not). | **M13 wave 5 ✅ (mostly)** — `_capped_history`/`_rolling_summary` (`brain/graph.py`) cap refine history to N=6 (`SENTINEL_REFINE_HISTORY_KEEP`) + a rolling-summary prefix; `ChatProjector` writes the summary to the chats domain. Retention for `conversations.db` (VACUUM / old-thread deletion) — ops/M13-service. |
| GAP-M9-21 | P3 | **No auto-escalate-to-HITL:** takeover today is only human/orchestrator-initiated (`checkpoint`→`rc.poll()`); on a run of heal/verify failures the agent does not summon a human itself. `graph.py`/`state.py` have NO failure counter. (Distinct from GAP-RISK-004 — that is a replay heal-retry cap.) | **M14 (in progress):** RunState `consecutive_heal_failures`/`failed_steps` + increment in heal/verify (reset on success); `route_checkpoint` arms `_takeover_armed` at `≥ SENTINEL_AUTO_HITL_THRESHOLD` (0=off) + a `hitl_needed` event over AG-UI. Live auto-pause verification = **M9-LIVE**. **Graph modes only for now** (explore/goal/describe/chat via `build_graph`); replay/baseline (`run_replay`) → **M14 tail 2 ✅**: AG-UI emission + counter + `hitl_needed` signal landed (`test_m14_replay_agui_offline.py`); the live auto-PAUSE (human takeover mid-replay) = M9-LIVE (no interrupt machinery). |

## Risks (full list; summary in ARCHITECTURE §8)

| ID | Priority | Risk | Mitigation |
|----|----------|------|------------|
| GAP-RISK-001 | P1 | `pw-executor` maintenance burden (build-only) — see GAP-ARCH-001 | thin layer + contract tests + pinned version |
| GAP-RISK-002 | P1 | Confidence-model cold start (no human-verified data when the store is being seeded with the most consequential records) | threshold 0.90 until N labeled; verify-before-accept + post-heal verify = model-independent gate; M2/M3 budget human review to bootstrap calibration set |
| GAP-RISK-003 | P2 | Token-cost blowout on large SPAs (50+ pages, Opus pricing) | coverage convergence (ADR-010), depth cap, per-page budget, graceful degrade to partial frozen plan, Go hard ceiling, incremental explore (skip unchanged-a11y-hash pages) |
| GAP-RISK-004 | P2 | Heal-storm latency/cost variance in deterministic replay hot path on a churning AUT | hard 2-attempt cap + per-step deadline + auto-skip; `dom_subtree_hash` amortization (recurrent change = 0 LLM after first heal); quarantine caps blast radius |
| GAP-RISK-005 | P2 | a11y-tree blind spots (shadow DOM, canvas, custom web components, cross-origin iframes) | `completeness_ratio` first-class metric (Grafana histogram) triggers visual fallback + surfaced in report; recommend AUT team add data-testid/ARIA where chronically low |
| GAP-RISK-006 | P2 | `dom_hash` fragility — whole-page hash invalidates all locators on unrelated change (ads/A-B/analytics) | hash target SUBTREE not page; configurable scope; CSS ignore-list for volatile widgets |
| GAP-RISK-007 | P2 | SQLite write contention under parallel CI / multi-runner K3s | per-job SQLite for CI (no shared writer); single Go-writer + WAL for service; documented Postgres trigger (>50 concurrent shared-DB writers / distributed workers) |
| GAP-RISK-008 | P3 | Proto/gRPC versioning friction as the brain evolves | CI-generated stubs from one `.proto`; proto-hash assertion (mismatch=build failure); optional fields + 1-major backward compat; boundary phased in only @ M2 |
| GAP-RISK-009 | P2 | Screenshot-hash not byte-stable across separate browser launches (baseline run vs replay run) → flaky visual golden-diff. M3 mitigations: capture once per page at first landing (no focus/caret), and make **visual regression advisory** (a11y-hash drives exit 2). Full fix deferred: fixed viewport + font set + `animations:'disabled'`/`caret:'hide'`, or capture goldens in the same process. **PARTIAL (M8):** determinism options implemented in pw-executor (`animations:'disabled'` + `caret:'hide'` + `scale:'css'` + fixed viewport 1280×720/DSR=1), tsc-verified; visual stays **advisory** until byte-stability is confirmed (golden twice in separate processes = equal). **Flip mechanism implemented (ADR-042, Phase A):** opt-in `SENTINEL_VISUAL_AUTHORITATIVE=1` gates exit 2 on a visual diff (`brain/replay.py`); default OFF = advisory (behavior unchanged). Determinism anchors → `pw-executor/src/determinism.ts` + `node:test` lock; both flag branches in `tests/test_determinism_offline.py`. **Default-on awaits the real headless-browser byte-stability proof (M9-LIVE).** | PARTIAL |
| GAP-RISK-010 | P1 | **Secret leak into the Playwright `trace.zip`** (M9.1/ADR-026): the trace records DOM snapshots (`input.value`, incl. `type=password`) AND the network POST body (the Keycloak login form). "Pause around `fill`" does not help — the secret leaves via the submit POST + the DOM snapshot; Playwright has no pause/mask API | **MITIGATED (M9.1):** the auth run sets `PW_NO_TRACE=1` → `context.tracing.start` is never called (its sole product is the `storageState` file); prod runs use `STORAGE_STATE` and never type the password → traces are clean by construction. **Fail-closed (not just env):** `browser.fill` throws while tracing is active (before reading the env secret), and the brain aborts the run (exit 3) on a `secretRef` step without `PW_NO_TRACE=1`. The secret in our artifacts (plan/transcript/heal-report/rec/OTel/stderr) is closed by `secretRef` (resolved only inside pw-executor) + log redaction and sanitized exceptions |
| GAP-OBS-001 | P3 | M4b deferrals: Go `report-service` HTTP `/metrics` (dropped — batch CronJob uses Pushgateway/textfile, ADR-018); TS (`pw-executor`) + Go (`store-gateway`) OTel spans with W3C context propagation; Go-side hard budget ceiling (needs a long-running Go orchestrator + brain→Go token reporting; default heuristic path uses no LLM). **RESOLVED (M8/ADR-021):** Go `report-service` (HTTP) + TS/Go OTel spans + W3C propagation (executor `_meta` + store.py gRPC interceptor + otelgrpc StatsHandler + per-node spans) + Go `orchestrator` budget-ceiling — implemented and **compile/test-verified** (Python 36 offline + `go build`/`vet`/`test` + `tsc`, all clean). Remaining to observe: a live OTLP trace + the real budget-kill end-to-end | RESOLVED |

## Operational & security gaps (Foundation cycle, 2026-06-27 → docs/THREAT_MODEL.md)

| ID | Priority | Gap | Goal / status |
|----|----------|-----|---------------|
| GAP-OPS-001 | P2 | **`pw-executor` lifecycle.** Playwright/Chromium is version-pinned (1.61.1) but there is no explicit upgrade cadence; tool name/schema contract tests are thin; multi-browser (Firefox/WebKit) is uncovered. Folds GAP-ARCH-001/RISK-001 (maintenance) + GAP-ARCH-007 (browser coverage — golden hashes differ per engine). | Goal: a documented upgrade cadence + contract tests on tool schemas. **Firefox/WebKit — RESOLVED (2026-06-28): Chromium-only by design (ADR-036)** — `connectOverCDP` is Chromium-only + golden hashes are per-engine; multi-engine = a separate decision-first milestone (not now). Owner: **ongoing** |
| GAP-OPS-002 | P2 | **AUT TLS certificate handling.** `browser.newContext` (pw-executor/src/server.ts:129) has no `ignoreHTTPSErrors` → an AUT with a bad cert (expired/self-signed/hostname-mismatch) yields a generic navigation error, not a classified one. | **DONE (this cycle):** opt-in `PW_IGNORE_HTTPS_ERRORS` (strict by default) + cert-error classification (`ERR_CERT*`/`ERR_SSL*`) in `browser.navigate` → an actionable diagnostic. Richer cert-diagnostic in heal-report — M9.4. |
| GAP-OPS-003 | P3 | **MCP-tool-wrapper signature drift (the explore() bug class, ADR-046).** Wrappers in `brain/server.py` call private `brain/__main__.py` helpers positionally; a helper signature change silently breaks the wrapper at runtime (M7 `explore()` → TypeError, caught only by the Inspector recipe; fixed in ADR-045 + 1 arity test). | Goal: arity locks (like `test_explore_tool_calls_run_explore_with_correct_arity`) for the `heal`/`replay`/`report` wrappers too; consider keyword calls. Owner: **ongoing** |
| GAP-OPS-004 | P3 | **The bilingual gate checks pair existence, not content.** `scripts/check_bilingual.py` catches a missing `.en.md` but not content divergence → historical drift (GAP-M9-13/14/15 were absent from EN; caught manually / by the verify workflow). | Goal: an optional content-parity check (heading/row-count or markers) OR make the verify workflow a required step of doc waves. Owner: **ongoing** |
| GAP-OPS-005 | P3 | **Two diverging implementations of the same RunConfig form.** `docs/setup/index.html` (after M11.5 PR-4 — the stepped schema-driven wizard, ADR-061) and the `#build` section of `docs/index.html` (M12 ph2, ADR-041) assemble the same RunConfig through different forms: different id prefixes (`b-*` vs none), a different field set (`#build` has no runtime presets / LLM_STRUCTURED / draft-persist), different validation. Drift between them is caught by nobody — the bilingual gate and `node --check` verify syntax, not parity. | Deferred deliberately (Alex, 2026-07-06): PR-4 rewrites **only** `setup/`, as DISTRIBUTION §7 prescribes. Goal: a separate milestone for convergence — either `setup/` becomes canonical and `#build` shrinks to a link/run-console, or the wizard is embedded into the hub as a top tab. Owner: **post-M11.5** |
| GAP-SEC-001 | P1 | **Full env inheritance + plaintext secrets in Helm.** `agentctl` passes `cmd.Env = os.Environ()` with no allowlist (cmd/agentctl/main.go:68) — every host secret is inherited by brain + children. The Helm CronJob injects env as plaintext `value:` (deploy/sentinel/templates/cronjob.yaml:34–46, incl. `CHECKPOINT_DSN` + `extraEnv`) — no `Secret`/`secretKeyRef`. | **CLOSED — Helm half (M11.3, ADR-035):** the agentctl env-allowlist is now **default-on** (opt-out `SENTINEL_ENV_ALLOWLIST=0`; `SENTINEL_ENV_ALLOW` for secretKeyRef names) — unit test `cmd/agentctl/main_test.go` confirms `AWS_SECRET_ACCESS_KEY` does not reach the brain. The Helm chart supplies the API key + DSN via `secretKeyRef` when `secrets.enabled` (plaintext fallback in dev/staging); the `sentinel.envAllow` helper auto-emits `SENTINEL_ENV_ALLOW`+`SENTINEL_ENV_ALLOWLIST=1`; Flux manifests `deploy/flux/` (v2 GA). AUT secrets already via `secretRef`+`PW_NO_TRACE` (GAP-RISK-010). **Remainder (outside M11.3, optional):** (1) **CLOSED (#25):** `NODE_`/`GIT_` removed from the agentctl allowlist — `NODE_AUTH_TOKEN`/`GIT_ASKPASS` no longer reach the brain; the legitimate `NODE_OPTIONS`/`NODE_EXTRA_CA_CERTS`/`GIT_SSL_CAINFO`/`GIT_SSL_CAPATH` are exact-allowlisted; unit test `TestFilteredEnvPrefixNarrowing`. (2) dynamic Vault/CSI-driver secrets — open. Detail: docs/DEVELOPMENT.md §Secret plumbing; docs/THREAT_MODEL.md |
| GAP-SEC-002 | P1 | **Supply chain.** Historically: no SCA in CI; Python deps with no committed lockfile; releases without signing/SBOM. | **PARTIALLY CLOSED (Foundation §1):** gitleaks (hard) + govulncheck (hard) + pip-audit (advisory + freeze artifact) + npm audit (critical) in CI + `go vet`/`go test` + the offline suite. **M11.1 (landed):** `.github/workflows/release.yml` provides SBOM (syft/CycloneDX) + **Cosign keyless** signatures (image + sign-blob of archives) + GitHub Releases + multi-arch GHCR; lockfile `brain/uv.lock` committed. **M11.4 (landed):** offline compose (`docker-compose.offline.yml`, `internal:true`/`pull_policy:never`, an offline anchor with no `build:`) + `scripts/offline-verify.sh` (`--local` CI gate: build→save/load→`--network none` demo+docs+negative-DNS) + `scripts/build-airgap-bundle.sh` (maintainer assembler) + CI `airgap` job; runtime with no Docker Hub/GHCR/npm/PyPI/GitHub **[core verified in CI]**. **Tail:** an actual signed release + the full bundle (GHCR image+model+signatures) on the first `v*` tag (E2E, maintainer-gated). Owner: **M11.1 + M11.4** |
| GAP-OPS-006 | P3 | **No on-disk "run finished" marker.** Neither `agentctl` nor the brain writes a completion signal: `report.json`/`report.html`/`metrics.prom` are produced by a separate `report` subcommand (`brain/__main__.py`), so "a run that died at step 3" and "a run still in flight" are indistinguishable on disk. Consequence: `scripts/collect-live-run.sh` cannot refuse to collect an unfinished run — it only warns about missing artifacts (deliberate: failing would be a false positive on a mid-flight run). | Goal: a marker (`status.json` carrying the exit code) written by `agentctl` on exit; the collector and the M15 dashboard could then tell a crash from in-flight. Owner: **post-M9-LIVE** |
| GAP-OPS-007 | P2 | **The cancel process-group invariant holds by convention, not by a boundary test.** `POST /v1/runs/{id}/cancel` (ADR-069) signals the GROUP, otherwise orphans remain (measured: 2 of 3 without the group, 0 with it). The group is created at spawn (`procgroup_unix.go`/`procgroup_windows.go`), but `cancel_test.go` checks cancellation behaviour and does NOT check that the leader's PGID differs from control-API's — so a regression introduced while refactoring spawn would send SIGKILL to the caller's group, hitting everything the operator started in the same shell session | `🟢OSS` | a boundary test: leader PGID ≠ service PGID; `docs/THREAT_MODEL.en.md` §4.12 |
| GAP-SEC-003 | P2 | **LLM authoring cannot emit `secretRef` → plaintext credentials in artifacts.** The authoring schemas (`brain/planner.py` `_SCHEMA_STEPS`/`_SCHEMA_DRAFT`) carry only `value` — there is no `secretRef` field — so a goal like "log in as user/password" materialises in `plan.json`/`scenario.json` (and in `trace.zip`) as a **literal password**. The deterministic `secretRef` path exists at runtime (`brain/scenario.py`), but the LLM cannot reach it. | **Mitigation (landed, M9-LIVE prep):** `scripts/collect-live-run.sh` blanks `value`/`text` on `fill\|type\|select\|press` steps without a `secretRef` by default; the `collect-live-run-smoke` CI job proves it with a canary. Goal: add `secretRef` to the authoring schema + a prompt rule against inlining credentials. Owner: **M9-LIVE / M10** |
| GAP-SEC-004 | P3 | **`STORAGE_STATE_SAVE` is not guarded against writing inside `runs/<id>`.** The path comes from the caller (`brain/runconfig.py` → `pw-executor/src/server.ts`) with no code-level "not into the artifact dir" barrier. A Playwright storage state is auth cookies + localStorage (session-hijack material); landing inside `runs/<id>/` it would ride out with any naive `tar runs/<id>`. | **Mitigation (landed, M9-LIVE prep):** the collector excludes `*state*.json` unconditionally (even with `--with-trace`) and warns loudly when one is present. Goal: reject a `storage_state` path inside the artifact dir in code (mirroring the `isUnder` guard in `cmd/agentctl`). Owner: **M10** |
| GAP-SEC-005 | P1 | **No WRITE-side redaction for the tested application's output.** Since ADR-067 seven `app.*` codes carry into `runs/<id>/logs/run.jsonl` whatever the foreign application printed: a `console.log` with a session token, a URL with `?token=`, dialog text with personal data, a 5xx body inside a message. `logsink.go` stores the message AS RECEIVED — there is no input filter on secret shapes (`Bearer `, `eyJ`, `?token=`, long hex/base64), unlike `brain`, which redacts its own logs. Only `0700` plus the read token protect it; no retention policy and no TTL exist for `logs/` (`trace.zip` has `SENTINEL_TRACE_KEEP`/`SENTINEL_TRACE_TTL_HOURS`, the logs do not) | `🟢OSS` | write-side redaction + TTL; `docs/THREAT_MODEL.en.md` §4.12 |
| GAP-UI-001 | P2 | **The LLM connection from the UI did not apply to runs.** The hub `#build` (`docs/index.html`) had LLM fields (backend/base_url/model/vision), but they only fed the `sentinel.env`/`run.yaml` generator; `spawnRun` took the LLM only from the control-API process env (`cmd.Env = os.Environ()`) — documented as deferred in ADR-062 (`readyz.go`). The self-contained co-pilot intent (configure via the UI) was unmet. | **CLOSED (ADR-063, 2026-07-23):** `spawnRun` materializes the LLM in layers process-env > per-run (`POST /v1/runs` field `llm`) > persisted (`/v1/config`); the hub `#build`/`#chat` send `llm`; the secret invariant is preserved (no api_key via the UI, `noauth` default for local openai, cloud key in the env); the per-run `base_url` is validated (SSRF, the same validator as `probeLLM`). Persisted = service tier (compose `store` profile). Tests `llmenv_test.go` + `llm_materialize_test.go`. Related: GAP-OPS-005 (form convergence `setup/` vs `#build`). |
| GAP-UI-002 | P2 | **The UI could not get its own access token; the split mode (`control-api`+`webui` on different origins) required manual CORS wiring.** Before ADR-064 the operator had to invent `CONTROL_API_TOKEN` themselves, copy it into Settings, and add the `webui` origin to `CONTROL_API_CORS_ORIGINS` — otherwise the browser blocked the cross-origin requests. | **CLOSED (ADR-064, 2026-07-23):** three deployment modes (headless / split / single-service) — the only difference between them is who serves the browser UI. There is no need to invent a token up front any more: if `CONTROL_API_TOKEN` is unset, control-api generates 32 random bytes and persists them to `state/control-api.token` (mode 0600, atomically), reusing the file on restart; `CONTROL_API_AUTOTOKEN=0` gives a deliberately tokenless read-only instance. The recommended mode 3 (`CONTROL_API_SERVE_UI=1` + an empty `CONTROL_API_CORS_ORIGINS`) serves the UI from the same origin as the API — the allowlist can stay empty, and the page picks up the address and token via a one-time `?bootstrap=<nonce>` link (valid 5 minutes) printed at startup. **Remaining limitation:** the bootstrap nonce is delivered only through the startup log — an operator who loses the terminal scrollback must read the token directly from `state/control-api.token`. |

## Product gaps (revision 2026-07-26 → docs/REGRESSION_MAP.en.md)

> Found by checking the code end to end against what the product promises. Each leads to a `PROD-*`
> item in `BACKLOG.md` and a row in `docs/REGRESSION_MAP.en.md`. Licence tier per ADR-056 §2.

| ID | Priority | Gap | Tier | Target / status |
|----|----------|-----|------|-----------------|
| GAP-PROD-001 | P1 | **`app.*` events never reach the verdict.** Seven application-fault codes (`js_error`/`console_error`/`console_warn`/`request_failed`/`http_error`/`dialog`/`log_capped`) are emitted by `pw-executor/src/server.ts:79-96`, parsed by `logsink.go` and written to `logs/run.jsonl` — and that is all: not in the artifact whitelist, not in `resultArtifact`, not in the exit code. A run reports `exit 0` PASSED while the application throws exceptions | `🟢OSS` | `[PROD-VERDICT-APP]`, its own ADR |
| GAP-PROD-002 | P1 | **Healing does not affect the verdict — UI drift is silent.** A run with N heals still exits 0; `healed` lives as a counter. The legitimate/illegitimate boundary sits inside `healing.py`: rotating `alternatives[]` re-binds the SAME element (repairing the test), while `_llm_reground`/`_visual_reground` pick a NEW selector and may bind a different element — there is no "is this the same element" check | `🟢OSS` | `[PROD-HEAL-VERDICT]`; precedent — testRigor `fixed-by-ai` diff |
| GAP-PROD-003 | P1 | **The coverage metric is structurally wrong.** It counts buttons ONLY (`graph.py`: `role == "button"`); links/fields/selects never enter the denominator. Worse: `interactive_seen` accumulates across pages, so going deeper GROWS the denominator and LOWERS coverage → the convergence condition `coverage>=0.85 AND frontier empty` is unreachable on a multi-page site and the run always hits `max_steps`=40 | `🟢OSS` | `[PROD-CRAWL]`, starts with a MEASUREMENT on a 50+ page SPA; absorbs GAP-RISK-003 |
| GAP-PROD-004 | P1 | **No SPA state deduplication and no return between exploration branches.** The frontier grows from same-origin `browser.links`, so a route change without an `<a href>` is invisible; two URLs rendering the same view count twice, one URL with different states counts once | `🟢OSS` | `[PROD-CRAWL]`; `state_hash` is necessary but not sufficient |
| GAP-PROD-005 | P1 | **No test versioning.** `plan_hash` is an integrity fingerprint, not a version; `TestRecord` is a single row with no revisions; `baseline` overwrites goldens with no history. Re-authoring erases the previous test without a trace, and there is nothing to roll an autonomous heal back with | `🟡SPLIT` | `[PROD-VERSIONING]`: revisions+rollback = Apache, long-retention = 🔒 |
| GAP-PROD-006 | P1 | **No import of existing tests.** No `.spec.ts` parser, no ts-morph/babel — verified by grep. Export is one-way (`exporter.py` → `@playwright/test`), recording only via our own MV3 recorder. The entrance is greenfield-only, while the team that pays already has a suite | `🟢OSS` | `[PROD-IMPORT]`; an imported step must pass grounding (ADR-022/028) |
| GAP-PROD-007 | P1 | **No JUnit XML.** Not a single reporter (`junit`/`testsuite`/`<testcase`) in `brain/`, `cmd/`, `pw-executor/src/`. CI integration is "read the exit code", so GitLab/Jenkins/GitHub/TeamCity show neither steps, nor timing, nor cause | `🟢OSS` | `[PROD-JUNIT]`, ~150 lines over the existing `heal-report.json` |
| GAP-PROD-008 | P2 | **No flake rate — only a binary quarantine.** "Failed 1 in 10" is not computed: there is no per-step history. This hits the literature's top category directly — Async Wait is first by frequency in Luo et al. (FSE'14) | `🟡SPLIT` | `[PROD-FLAKE-RATE]`: basic trends = Apache, ML scoring = 🔒 (ADR-056 §2) |
| GAP-PROD-009 | P3 | **Performance is absent as a check axis.** Only whole-run `duration_ms` is measured; "performance efficiency" is one of the eight characteristics of ГОСТ Р ИСО/МЭК 25010-2015 and is not covered at all | `🟡SPLIT` | `[PROD-PERF]`; the instrumentation already exists (CDP metrics, the network timeline in `trace.zip`) |
| GAP-PROD-010 | P3 | **Accessibility is used as a HASH, not as a criterion.** `_a11y_hash` (`replay.py:41`) catches "the tree changed" but not "it became inaccessible": a button with no name, a field with no label, contrast, focus order — all missed. The reference is normative: ГОСТ Р 52872-2019 (WCAG 2.1), a requirement for the Russian public sector | `🟢OSS` | `[PROD-A11Y]` |
| GAP-PROD-011 | P3 | **One browser engine; cross-browser needs goldens PER ENGINE.** ADR-036 is Chromium-only deliberately: `connectOverCDP` is Chromium-only, and golden hashes differ per engine — a Chromium golden would give `exit 2` in Firefox for no reason | `🟡SPLIT` | `[PROD-ENGINES]`; absorbs GAP-OPS-001 |
| GAP-PROD-012 | P3 | **The MV3 recorder is not in CI.** `extension/` only builds locally (esbuild) and live record→replay has never been verified — yet it is one of the product's two entrances | `🟢OSS` | `[M9.8]`; paired with `[PROD-IMPORT]` — both are about entering an existing project |
