# GAPS — Sentinel

> 🌐 **Русский** (основная версия) · [English](GAPS.en.md)

Отслеживание открытых вопросов, пунктов для верификации и известных рисков. Формат: `GAP-[CAT]-[NUM]`.
Категории: ARCH (архитектура), VERIFY (требует проверки фактов), RISK, AGENT (отсутствующий инструментарий), DECISION (ожидание решения пользователя).

> Источник: синтез workflow дизайна (2026-06-23) + согласование ограничений BUILD-ONLY.

---

## Пробелы в решениях / ограничениях

| ID | Приоритет | Пробел | Статус |
|----|-----------|--------|--------|
| GAP-DECISION-001 | P1 | **Интерпретация BUILD-ONLY.** OSS-библиотеки (Playwright, LangGraph, Anthropic SDK, MCP SDK) = разрешены (это «написание»); готовые серверные продукты/SaaS = не разрешены. | **RESOLVED 2026-06-23** (подтверждено пользователем). GAP-ARCH-002 разблокирован → закрыт. |
| GAP-ARCH-001 | P1 | **`pw-executor` теперь на критическом пути.** Создание и поддержка собственного TypeScript Playwright execution server — самый большой отдельный компонент и наибольший риск постоянного обслуживания (отслеживает изменения API библиотеки Playwright). Снижение риска: тонкий слой инструментов поверх стабильного API `Locator`/`accessibility` Playwright; контрактные тесты, проверяющие имена инструментов и схемы; фиксация версии Playwright. | OPEN |
| GAP-ARCH-002 | P2 | Если GAP-DECISION-001 разрешится в «никакого OSS тоже», придётся переоценить весь стек (LangGraph → собственный цикл, Playwright → raw CDP). Аннулирует ADR-002/004/005. | BLOCKED on GAP-DECISION-001 |

## VERIFY (проверка фактов — anti-hallucination; не предполагать)

| ID | Приоритет | Пункт | Устранить к |
|----|-----------|-------|-------------|
| GAP-VERIFY-001 | P1 | Возможности базового **Playwright library API** для зафиксированной версии: поверхность `accessibility` snapshot, `tracing.start/stop`, движки локаторов (role/text/label/testid), `screenshot`. Мы ОПРЕДЕЛЯЕМ собственную поверхность инструментов в `pw-executor`, но она опирается на эти примитивы. | M0 |
| GAP-VERIFY-002 | P1 | Пакет **Python↔MCP binding** и его зрелость (например, `langchain-mcp-adapters`): стабильность транспорта stdio, строгость валидации схем инструментов, распространение ошибок subprocess. Оба конца теперь наши (снижает риск). Сохраняем тонкий заменяемый адаптер, чтобы кастомный JSON-RPC клиент на ~300 строк оставался запасным вариантом с низким риском. | M1 |
| GAP-VERIFY-003 | P2 | API checkpointer **LangGraph `SqliteSaver` / `AsyncPostgresSaver`** + использование отдельного DB-файла; семантика `interrupt`/pause для human gate. | M1 |
| GAP-VERIFY-004 | P3 | Формат вызовов structured-output / tool-use **Anthropic SDK** для узлов plan + heal (ID моделей `claude-opus-4-8`, `claude-sonnet-4-6`). | M1 · **✅ RESOLVED 2026-07-05 (ADR-057)** — plan/heal используют Anthropic forced `tool_use` (`complete_json`, `brain/llm.py`) + OpenAI `json_schema`; offline-verified + adversarial-review; live-provider smoke → GAP-VERIFY-005/M9-LIVE. |
| GAP-VERIFY-005 | P2 | **Реальный smoke провайдер-агностичного backend (M6/ADR-019).** Сеть в среде заблокирована → offline покрыт `FakeBackend`; реальное поведение OpenAI-compat (минимум один роутер: OpenRouter/DeepSeek/Qwen/Gemini-compat) — **user-run**: `temperature=0` принимается?; `max_tokens` vs `max_completion_tokens` (o-series); отсутствие `usage` (Ollama/vLLM); vision `image_url` data-URI. Инструкция в `docs/M6_CONTRACT.md`. **LiteLLM-роутер задокументирован (ADR-045):** `docs/ADAPTERS.md` + compose `litellm`-профиль — конкретный OpenAI-compat роутер для этого smoke. | M6 (user-run) |
| GAP-VERIFY-006 | P2 | **Поддержка MCP `sampling/createMessage` по хостам** для M7 `SamplingBackend` (ADR-020): M7 реализован, offline-verified; серверный `create_message` API подтверждён на установленном `mcp`. Остаётся **user-run** — реальный хост (Claude Desktop — да; **OpenCode/Kilocode — подтвердить capability перед боевым использованием**). Нет sampling → backend недоступен → fallback на heuristic/L1–L6. **MCP-Inspector задокументирован (ADR-045, `docs/ADAPTERS.md` §3)** как stdio-хост: `tools/list` (4 tools) + live `sampling/createMessage` — конкретный verification-path; живой прогон по хостам остаётся user-run. | M7 (user-run) |

## Открытые вопросы дизайна (из синтеза)

| ID | Приоритет | Вопрос |
|----|-----------|--------|
| GAP-ARCH-003 | P1 | Точная **метрика покрытия** для ADR-010: «интерактивный элемент задействован» считается по `semantic_id` / по `(page,role,name)` / по отдельному flow? Не должна поощрять тривиальные клики и не должна штрафовать небольшие приложения. |
| GAP-ARCH-004 | P1 | Как надёжно определить целевое **SUBTREE** сценария для скоупинга `dom_subtree_hash` (ближайший landmark/role container?) без избыточного или недостаточного охвата. Проверить на реальном DOM drift. |
| GAP-ARCH-005 | P2 | **Объём начальной калибровки:** сколько верифицированных человеком исходов нужно до снижения порога auto-accept с 0.90 по умолчанию, и за какой период? |
| GAP-ARCH-006 | P2 | Стоит ли **мягкая верификация на Sonnet** в режиме explore своих затрат, или достаточно детерминированной проверки состояния после действия (изменение URL, наличие ожидаемого элемента)? Измерить на M3. |
| GAP-ARCH-007 | P2 | **Охват браузеров:** для MVP предполагается только Chromium; подтвердить краткосрочную потребность в Firefox/WebKit (влияет на переносимость golden-baseline — хэши различаются для каждого движка). **Решено (2026-06-28): Chromium-only by design (ADR-036)** — Firefox/WebKit отложены (GAP-OPS-001). |
| GAP-ARCH-008 | P1 | **Обработка auth/секретов** для AUT (внедрение storage-state/cookie): где хранятся учётные данные (home-lab Vault?) и как они указываются в `RunConfig` без попадания в traces/транскрипты? |

> **Re-tag (Foundation cycle, 2026-06-27):** GAP-ARCH-003..006 переходят в фазу **live-verify** (трекаются под **M9-LIVE** — измеряются на живом прогоне, не offline). GAP-ARCH-007 (охват браузеров) свёрнут в **GAP-OPS-001** (lifecycle pw-executor). GAP-ARCH-008 (auth/секреты) частично закрыт M9.1 (`secretRef`+`storageState`); остаток (Vault/Helm Secret) → **GAP-SEC-001** / M11.3.

## M9 — пробелы возможностей (дизайн-сессия 2026-06-26 → docs/M9_CONTRACT.md)

| ID | Приоритет | Пробел | Цель |
|----|-----------|--------|------|
| GAP-M9-01 | P1 | `fill`/`type`/`press`/`select` в pw-executor отсутствуют → нет форм/поиска/**логина** | M9.1 (блокер №1) — **DONE offline 2026-06-26 (ADR-026):** 6 tools (+`expect`/`saveStorageState`) в обоих транспортах; tsc+FakeEx green. Живой UI — по «go» |
| GAP-M9-02 | P1 | `GoalPlanner` (NL→plan, explore-first grounding) + `--mode explore\|goal\|describe` + авто-дефолт | M9.2a (goal-режим + `grounding` по индексу, ADR-027) + M9.2b (двухфазный explore-then-scenario §L + describe-first §B + one-shot `build_scenario`/`reconcile`, ADR-028) **DONE offline**. Живой goal/describe-прогон — по «go» |
| GAP-M9-03 | P2 | Не-MCP HTTP/gRPC control-API + OSS чат-UI в DH (плюс MCP-путь через M7) | M9.3: control-API (`cmd/control-api`, ADR-032) + setup-WebUI live-mode **DONE** (Волна B p1/p2); OSS чат-UI (`docs/chat/index.html`) + CI-шаблоны (`docs/ci-templates/`) — **DONE (M9.3-tail, ADR-040)**. M9.3 завершён |
| GAP-M9-04 | P2 | Auth-adapter (storageState + Keycloak/OIDC), креды из Vault; **логин как тест-цель** | M9.1 (storageState load/save + login-as-test + `secretRef`) **DONE offline**; pluggable auth-adapter + Vault/Helm Secret — M9.7 |
| GAP-M9-05 | P2 | In-app tabs (perception `role=tab`/`tabpanel`) + browser multi-tab/context (multi-page в pw-executor) | M9.4 **DONE offline (Волна A)**: `[role=tab]` в interactives/setOfMarks (A5) + multi-page (`browser.tabs`/`switchTab`, `context.on('page')`, A6). Живой multi-tab + brain «план ссылается на вкладку» — по «go» |
| GAP-M9-06 | P2 | Инъекция `traceparent` во все запросы браузера → корреляция UI-теста с backend/Kafka-trace в Tempo | M9.5 **DONE offline (Волна A)**: `context.route('**/*')` инъекция `traceparent` (gated на OTLP, no-op без коллектора). Живая backend-корреляция — по «go» (нужен коллектор + OTel-AUT) |
| GAP-M9-07 | P2 | Режимы браузера: headed + CDP-attach к браузеру пользователя (`connectOverCDP`) + co-pilot takeover/return | **M9.6 DONE offline (Волна D, ADR-037):** headed (`PW_HEADLESS=0`) + CDP-attach (`PW_CDP_ENDPOINT` — переиспользует контекст пользователя, teardown не закрывает его браузер) в `pw-executor` (`launch.ts`+`server.ts`), env-toggle без новых RPC; co-pilot takeover/return → **M9.8**; live-verify (видимое окно / реальный CDP) по «go». Контракт `M9.6_CONTRACT.md` |
| GAP-M9-08 | P3 | Pluggable adapters (auth/deploy/model/backend) — универсальность не-только-DH | **model/backend-адаптер адресован (ADR-045):** LiteLLM опц-роутер за `LLM_BASE_URL` + зонтичный `docs/ADAPTERS.md`. Остаток расщеплён (ADR-056 §2): **open-core auth/deploy-adapter-SPI + базовый адаптер → M9.7**; **enterprise-auth (Keycloak/OIDC/Vault/SSO/RBAC) → `[M-COMMERCIAL-auth]`** (🔒 private `sentinel-enterprise`, НЕ Apache) |
| GAP-M9-09 | P2 | RunConfig-файл (YAML) + config-surfaces: режим/goal/auth/бюджеты через флаги · env · файл · интерактивно (чат). Сейчас только флаги+env, per-run | M9.2a (минимальный RunConfig: mode/goal/planner/budgets) + M9.2b (богатый: декларативные `auth:`→STORAGE_STATE*/PW_NO_TRACE + именованные `scenarios:` + `--scenario`-селектор, ADR-028) **DONE offline**; per-role model-endpoints — M9.7; chat-surface — M9.3 |
| GAP-M9-10 | P2 | Validation / негативное тестирование: генератор невалидных вводов по типу/маске поля + assert-слой («UI отверг ввод») | M9.1 (assert-примитив `browser.expect` + `expect_ok`-полярность + `brain/validation.py` **набросок**) **PARTIAL — DONE offline**; полный генератор (маски/границы/схема) — M9.2 |
| GAP-M9-11 | P3 | **Security-модуль (M10, отдельный):** XSS/CSRF/IDOR/auth-bypass/sensitive-data-in-DOM поверх explore-карты; **authorization-gated** | M10 |
| GAP-M9-12 | P3 | CI-шаблоны: Jenkinsfile + `.gitlab-ci.yml` (Sentinel = CLI + exit-коды → любой CI на коммит) | M9.3 — **DONE (M9.3-tail)**: `docs/ci-templates/{Jenkinsfile,.gitlab-ci.yml,README+en}`, маппинг exit 0/1/2/3 |
| GAP-M9-13 | P2 | **Рекордер действий (расширение M9.8):** content-script захват click/input → события → grounding в шаги (reuse M9.2b `reconcile`). Сейчас нет (есть describe-mode NL→сценарий, но не event-capture). Блокер реализации M9.8 | M9.8 (ADR-038). **Транспорт готов (M9.8-prep, WS `/v1/stream`, ADR-043).** **✅ DONE:** `extension/` (#42-46) — content-script рекордер с обязательной redaction + `brain/record_bridge.py` (record→scenario) + recorder-e2e. Dev-only; live-replay = M9-LIVE |
| GAP-M9-14 | P2 | **Стриминг-транспорт extension↔brain (M9.8):** control-API (M9.3) только request/response (нет WS/SSE). Нужен стриминг-эндпоинт (или native-messaging host). Блокер M9.8 | **DONE:** server→client **SSE** `GET /v1/runs/{id}/events` (ADR-040) + client→server **hand-rolled WS** `GET /v1/stream` (M9.8-prep, ADR-043 — recorder NDJSON ingest → `events.ndjson`, токен via `Sec-WebSocket-Protocol`, 6 httptest race). reuse ADR-032 security. native-messaging — альтернатива для air-gapped (не реализована) |
| GAP-M9-15 | P2 | **Takeover/return протокол (M9.8):** передача управления агент↔человек на одной CDP-сессии (поверх M9.6). Не определён. Блокер co-pilot | M9.8 (ADR-039). **Транспорт (WS `/v1/stream`, ADR-043) + фронт-скелет (ADR-044) готовы.** **MV3 takeover/return ✅ DONE** (#47, `extension/src/background/takeover.ts` — `chrome.debugger` attach/return, видимый баннер, auto-detach, reconcile); **brain interrupt/resume ✅ DONE** (R3/ADR-054, GAP-M9-18) |
| GAP-M9-16 | P2 | **Replay/baseline ВНУТРИ UI (M9.9/R1, ADR-046):** запуск/перепрогон готового `scenario.json`/`plan.json` + фиксация baseline из чата/setup, не только CLI/CI. Отложено в M9.3 как spawn-поверхность; **un-deferred** | **M9.9 [me]** — control-API `mode=replay\|baseline` + `from_run:<run_id>` (whitelist+traversal-guard, не произвольный путь) + ▶/🔁/📌 в vanilla-UI + вердикт. Плумбинг оффлайн; live replay = M9-LIVE. **✅ DONE: R1a backend (ADR-047: control-API + 12 httptest) + R1b UI (▶/🔁/📌 + вердикт в `#build`/`#chat`/`chat/`/`setup/`).** |
| GAP-M9-17 | P2 | **Multi-turn conversational authoring (M9.10/R2, ADR-046):** сейчас всё one-shot (1 сообщение = 1 прогон); нет контекста между тёрнами и коррекции по ходу. Была безномерная «brain-extension веха» | **M9.10 [me]** — brain `chat` `RUN_MODE` поверх готового LangGraph-checkpointer (стабильный `conversation_id`→`thread_id`) + `messages` add_messages-канал + мульти-тёрн-панель. **✅ CLOSED — R2a backend (ADR-048): conversation_id→thread_id resume, conditional-entry refine, общий `state/conversations.db`, agentctl/control-API `conversation_id`, offline two-turn тест + R2b UI: vanilla `docs/chat`+`docs/index#chat` mint `conversation_id`/разговор, кумулятивный тред, 🆕 «Новый разговор», переписаны one-shot-дисклеймеры. Live=M9-LIVE.** |
| GAP-M9-18 | P2 | **F4 takeover execution-side (brain) (M9.8/R3, ADR-046):** brain не умеет паузить прогон по takeover-сигналу и резюмить по return со снятием state-delta («live-переключений посреди прогона нет»). Парный к extension #47 | **✅ DONE (R3, ADR-054, 2026-06-30)** — RunControl `Control.takeover` + `Takeover`/`Return` RPC; brain `checkpoint`-арм → выделенный `takeover`-узел `interrupt()`/`Command(resume)` (латч `_takeover_armed`); `_resume_through_takeovers`; control-API `/v1/stream` форвард `{type:control,action:takeover\|return}` → orchestrator. Offline fake-client тест (`tests/test_r3_takeover_offline.py` + Go). Живой e2e (extension #47 + CDP) = M9-LIVE. Закрывает brain-половину GAP-M9-15 |
| GAP-M9-19 | P3 | **Stale-site-map в refine (M9.10-follow-up):** warm refine-тёрн авторит над `site_map` каким он был на cold turn-1; дрейф AUT между тёрнами не виден → можно авторить против устаревшей карты. | **M13 wave 6 ✅ (частично)** — opt-in `SENTINEL_REFINE_REVERIFY=1` форсит cold re-explore на warm-нити (`brain/__main__.py`), обновляя устаревший `site_map`. Auto-detect (a11y-хэш текущей страницы vs persisted на warm-тёрне) требует браузер — которого у warm-тёрна нет по дизайну → **M9-LIVE**. До того «новый разговор» / reverify = свежий explore. |
| GAP-M9-20 | P3 | **Необрезанная conversation-история (M9.10-follow-up):** R2a `messages`-канал + `_user_turns` отдают ВСЕ user-тёрны; `conversations.db` копит всё → промпт/чекпойнт раздувается на длинных разговорах (per-LLM-call ограничен `[:8000]`/`max_tokens`, но история — нет). | **M13 wave 5 ✅ (в основном)** — `_capped_history`/`_rolling_summary` (`brain/graph.py`) cap refine-history до N=6 (`SENTINEL_REFINE_HISTORY_KEEP`) + rolling-summary-prefix; `ChatProjector` пишет summary в chats-домен. Retention `conversations.db` (VACUUM/удаление старых нитей) — ops/M13-service. |
| GAP-M9-21 | P3 | **Нет auto-escalate-to-HITL:** takeover сегодня инициирует только человек/оркестратор (`checkpoint`→`rc.poll()`); при серии неудач heal/verify агент не зовёт человека сам. В `graph.py`/`state.py` НЕТ счётчика неудач. (Отдельно от GAP-RISK-004 — тот = replay heal-retry-лимит.) | **M14 (в работе):** RunState `consecutive_heal_failures`/`failed_steps` + инкремент в heal/verify (сброс на успехе); `route_checkpoint` арм `_takeover_armed` при `≥ SENTINEL_AUTO_HITL_THRESHOLD` (0=off) + `hitl_needed`-событие в AG-UI. Живая проверка авто-паузы = **M9-LIVE**. **Сейчас только graph-режимы** (explore/goal/describe/chat через `build_graph`); replay/baseline (`run_replay`) → **M14 tail 2 ✅**: AG-UI-эмиссия + counter + `hitl_needed`-сигнал приземлены (`test_m14_replay_agui_offline.py`); живая auto-ПАУЗА (human-takeover mid-replay) = M9-LIVE (нет interrupt-механизма). |

## Риски (полный список; итог в ARCHITECTURE §8)

| ID | Приоритет | Риск | Снижение риска |
|----|-----------|------|----------------|
| GAP-RISK-001 | P1 | Нагрузка по обслуживанию `pw-executor` (build-only) — см. GAP-ARCH-001 | тонкий слой + контрактные тесты + зафиксированная версия |
| GAP-RISK-002 | P1 | **Модели уверенности не существует** (формулировка исправлена 2026-07-26 — «cold start» занижала проблему). `brain/healing.py` берёт число из статического словаря `PRIORS` по ИМЕНИ стратегии; ни `healing_audit`, ни что-либо ещё не влияет на него, и обучаться не на чем: размеченных человеком исходов ноль. | **СМЯГЧЕНИЯ НЕТ.** Ранее здесь стояло «порог 0.90 до N размеченных записей» — такого механизма в коде никогда не было: `AUTO, FLAG = 0.85, 0.60`, плоские константы. ADR-080 сделал их env-настраиваемыми и явно назвал приорами, а не вероятностями; ввёл правило «переоснова не принимается молча» и вывел применённые-без-уверенности починки на вердикт. Калибровка возможна только после сбора размеченных исходов — отдельный пункт бэклога. |
| GAP-RISK-003 | P2 | Взрывной рост стоимости токенов на больших SPA (50+ страниц, ценообразование Opus) | convergence покрытия (ADR-010), ограничение глубины, бюджет на страницу, плавная деградация до частично замороженного плана, жёсткий потолок Go, инкрементальное исследование (пропуск страниц с неизменённым a11y-hash) |
| GAP-RISK-004 | P2 | Дисперсия задержки/стоимости heal-storm в детерминированном replay hot path при активно изменяющемся AUT | жёсткий лимит в 2 попытки + дедлайн на шаг + автопропуск; амортизация `dom_subtree_hash` (повторяющееся изменение = 0 запросов к LLM после первого heal); карантин ограничивает радиус поражения |
| GAP-RISK-005 | P2 | Слепые зоны a11y-tree (shadow DOM, canvas, кастомные web-компоненты, cross-origin iframes) | **СМЯГЧЕНИЯ НЕТ.** Ранее здесь стояло «`completeness_ratio` — метрика первого класса (Grafana histogram), запускает visual fallback» — `grep -rni completeness` по `*.py/*.ts/*.go` даёт **ноль** совпадений. Восприятие — фиксированный CSS-список в `browser.interactives`. Visual fallback существует (Tier-7), но не привязан ни к какой метрике полноты и выключен по умолчанию. |
| GAP-RISK-006 | P2 | Хрупкость `dom_hash` — хэш всей страницы аннулирует все локаторы при несвязанном изменении (реклама/A-B/аналитика) | хэшировать целевое SUBTREE, а не страницу; настраиваемый скоуп; CSS-список игнорирования для волатильных виджетов |
| GAP-RISK-007 | P2 | Конкуренция за запись в SQLite при параллельном CI / multi-runner K3s | per-job SQLite для CI (без общего writer); единственный Go-writer + WAL для сервиса; задокументированный триггер перехода на Postgres (>50 параллельных shared-DB writers / distributed workers) |
| GAP-RISK-008 | P3 | Сложности версионирования Proto/gRPC по мере развития brain | **СМЯГЧЕНИЯ НЕТ.** Ранее здесь стояло «стабы генерируются CI из одного `.proto`; проверка proto-hash = ошибка сборки» — `grep proto .github/workflows/*.yml` даёт **ноль**: генерации нет ни в одном job'е, стабы правятся руками и коммитятся, `protoc` даже не установлен в окружении разработки. |
| GAP-RISK-009 | P2 | Screenshot-hash нестабилен побайтно при отдельных запусках браузера (baseline run vs replay run) → нестабильный visual golden-diff. Снижение риска на M3: захват один раз на страницу при первом посещении (без фокуса/каретки), и сделать **visual regression advisory** (a11y-hash управляет exit 2). Полное исправление отложено: фиксированный viewport + набор шрифтов + `animations:'disabled'`/`caret:'hide'`, или захват goldens в одном процессе. **PARTIAL (M8):** determinism-опции реализованы в pw-executor (`animations:'disabled'` + `caret:'hide'` + `scale:'css'` + фикс-viewport 1280×720/DSR=1), tsc-verified; visual остаётся **advisory** до подтверждения байт-стабильности (golden дважды в разных процессах = equal). **Flip-механизм реализован (ADR-042, Фаза A):** opt-in `SENTINEL_VISUAL_AUTHORITATIVE=1` гейтит exit 2 на визуальный diff (`brain/replay.py`); default OFF = advisory (поведение не изменилось). Determinism-якоря → `pw-executor/src/determinism.ts` + lock `node:test`; обе ветки флага в `tests/test_determinism_offline.py`. **Default-on ждёт реального byte-stability proof на headless-браузере (M9-LIVE).** | PARTIAL |
| GAP-RISK-010 | P1 | **Утечка секретов в Playwright `trace.zip`** (M9.1/ADR-026): трейс пишет DOM-снапшот (`input.value`, вкл. `type=password`) И тело сетевого POST (форма логина Keycloak). «Пауза вокруг `fill`» не помогает — секрет уходит на submit-POST + в DOM-снапшот; pause/mask API у Playwright нет | **MITIGATED (M9.1):** auth-прогон ставит `PW_NO_TRACE=1` → `context.tracing.start` не вызывается (единственный продукт — `storageState`-файл); прод-прогоны идут с `STORAGE_STATE`, пароль не вводится → трейсы чисты by construction. **Fail-closed (не только env):** `browser.fill` бросает при активном трейсе (до чтения env-секрета), brain аварийно завершает прогон (exit 3) при `secretRef`-шаге без `PW_NO_TRACE=1`. Секрет в наших артефактах (plan/transcript/heal-report/rec/OTel/stderr) закрыт `secretRef` (резолв только в pw-executor) + редакцией логов и санитайзингом исключений |
| GAP-OBS-001 | P3 | Отложенные пункты M4b: Go `report-service` HTTP `/metrics` (убран — batch CronJob использует Pushgateway/textfile, ADR-018); TS (`pw-executor`) + Go (`store-gateway`) OTel spans с W3C context propagation; жёсткий потолок бюджета на стороне Go (требует долгоживущего Go orchestrator + отчётности brain→Go о токенах; путь эвристики по умолчанию не использует LLM). **RESOLVED (M8/ADR-021):** Go `report-service` (HTTP) + TS/Go OTel spans + W3C propagation (executor `_meta` + store.py gRPC interceptor + otelgrpc StatsHandler + per-node spans) + Go `orchestrator` budget-ceiling — реализованы и **compile/test-verified** (Python 36 offline + `go build`/`vet`/`test` + `tsc` — всё clean). Остаётся observe: live OTLP-trace + реальный budget-kill end-to-end | RESOLVED |

## Операционные и security-пробелы (Foundation cycle, 2026-06-27 → docs/THREAT_MODEL.md)

| ID | Приоритет | Пробел | Цель / статус |
|----|-----------|--------|----------------|
| GAP-OPS-001 | P2 | **Lifecycle `pw-executor`.** Версия Playwright/Chromium закреплена (1.61.1), но нет явной политики upgrade-cadence; контрактные тесты на имена/схемы инструментов тонкие; multi-browser (Firefox/WebKit) не покрыт. Свёртывает GAP-ARCH-001/RISK-001 (поддержка) + GAP-ARCH-007 (охват браузеров — golden-хэши различаются per-engine). | Цель: задокументированная upgrade-cadence + contract-tests на tool-схемы. **Firefox/WebKit — РЕШЕНО (2026-06-28): Chromium-only by design (ADR-036)** — `connectOverCDP` только Chromium + golden-хэши per-engine; multi-engine = отдельная decision-first веха (не сейчас). Owner: **ongoing** |
| GAP-OPS-002 | P2 | **Обработка TLS-сертификата AUT.** `browser.newContext` (pw-executor/src/server.ts:129) без `ignoreHTTPSErrors` → AUT с bad-cert (expired/self-signed/hostname-mismatch) даёт generic navigation error, не классифицированную ошибку. | **DONE (этот цикл):** opt-in `PW_IGNORE_HTTPS_ERRORS` (по умолчанию строго) + классификация cert-ошибок (`ERR_CERT*`/`ERR_SSL*`) в `browser.navigate` → внятная диагностика. Расширенный cert-diagnostic в heal-report — M9.4. |
| GAP-OPS-003 | P3 | **Дрейф сигнатур MCP-tool-обёрток (класс explore()-бага, ADR-046).** Обёртки в `brain/server.py` вызывают приватные хелперы `brain/__main__.py` позиционно; смена сигнатуры хелпера молча ломает обёртку в рантайме (M7 `explore()` → TypeError, пойман только Inspector-рецептом, пофикшен ADR-045 + 1 arity-тест). | Цель: arity-локи (как `test_explore_tool_calls_run_explore_with_correct_arity`) для `heal`/`replay`/`report`-обёрток тоже; рассмотреть keyword-вызовы. Owner: **ongoing** |
| GAP-OPS-004 | P3 | **Bilingual-гейт проверяет только наличие пары, не контент.** `scripts/check_bilingual.py` ловит отсутствие `.en.md`, но не расхождение содержимого → исторический drift (GAP-M9-13/14/15 отсутствовали в EN; ловилось вручную/verify-воркфлоу). | Цель: опц. контент-parity-чек (heading/row-count или маркеры) ИЛИ зафиксировать verify-workflow как обязательный шаг doc-волн. Owner: **ongoing** |
| GAP-OPS-005 | P3 | **Две расходящиеся реализации одной RunConfig-формы.** `docs/setup/index.html` (после M11.5 PR-4 — пошаговый schema-driven мастер, ADR-061) и секция `#build` в `docs/index.html` (M12 ph2, ADR-041) собирают один и тот же RunConfig разными формами: разные id-префиксы (`b-*` vs без), разный набор полей (у `#build` нет пресетов рантаймов / LLM_STRUCTURED / draft-persist), разная валидация. Дрейф между ними никем не ловится — bilingual-гейт и `node --check` проверяют синтаксис, не паритет. | Решение отложено осознанно (Alex, 2026-07-06): PR-4 переписывает **только** `setup/`, как и предписывает DISTRIBUTION §7. Цель: отдельная веха на конвергенцию — либо `setup/` становится каноном и `#build` ужимается до ссылки/run-console, либо wizard встраивается в хаб top-tab'ом. Owner: **пост-M11.5** |
| GAP-SEC-001 | P1 | **Полное наследование env + plaintext-секреты в Helm.** `agentctl` передаёт `cmd.Env = os.Environ()` без allowlist (cmd/agentctl/main.go:68) — каждый хост-секрет наследуется brain+детьми. Helm CronJob инжектит env как plaintext `value:` (deploy/sentinel/templates/cronjob.yaml:34–46, вкл. `CHECKPOINT_DSN` + `extraEnv`) — без `Secret`/`secretKeyRef`. | **CLOSED — Helm-половина (M11.3, ADR-035):** env-allowlist в agentctl теперь **default-on** (opt-out `SENTINEL_ENV_ALLOWLIST=0`; `SENTINEL_ENV_ALLOW` для secretKeyRef-имён) — unit-тест `cmd/agentctl/main_test.go` подтверждает, что `AWS_SECRET_ACCESS_KEY` не доходит до brain. Helm chart подаёт API-ключ+DSN через `secretKeyRef` при `secrets.enabled` (plaintext-fallback в dev/staging), helper `sentinel.envAllow` авто-эмитит `SENTINEL_ENV_ALLOW`+`SENTINEL_ENV_ALLOWLIST=1`; Flux-манифесты `deploy/flux/` (v2 GA). AUT-секреты уже через `secretRef`+`PW_NO_TRACE` (GAP-RISK-010). **Остаток (вне M11.3, опц.):** (1) **CLOSED (#25):** `NODE_`/`GIT_` убраны из agentctl-allowlist — `NODE_AUTH_TOKEN`/`GIT_ASKPASS` больше не доходят до brain; легитимные `NODE_OPTIONS`/`NODE_EXTRA_CA_CERTS`/`GIT_SSL_CAINFO`/`GIT_SSL_CAPATH` exact-allowlisted; unit-тест `TestFilteredEnvPrefixNarrowing`. (2) динамические секреты Vault/CSI-driver — открыто. Детали: docs/DEVELOPMENT.md §Secret plumbing; docs/THREAT_MODEL.md. **Live-verify** (kubectl describe / Flux reconcile) — на home-lab K3s. |
| GAP-SEC-002 | P1 | **Supply-chain.** Исторически: нет SCA в CI; Python-deps без committed lockfile; релизы без подписи/SBOM. | **ЧАСТИЧНО ЗАКРЫТО (Foundation §1):** gitleaks (hard) + govulncheck (hard) + pip-audit (advisory + freeze-артефакт) + npm audit (critical) в CI + `go vet`/`go test` + offline-suite. **M11.1 (landed):** `.github/workflows/release.yml` даёт SBOM (syft/CycloneDX) + **Cosign-keyless**-подписи (образ + sign-blob архивов) + GitHub Releases + multi-arch GHCR; lockfile `brain/uv.lock` committed. **M11.4 (landed):** offline compose (`docker-compose.offline.yml`, `internal:true`/`pull_policy:never`, offline-anchor без `build:`) + `scripts/offline-verify.sh` (`--local` CI-гейт: build→save/load→`--network none` demo+docs+negative-DNS) + `scripts/build-airgap-bundle.sh` (maintainer-ассемблер) + CI `airgap`-job; runtime без Docker Hub/GHCR/npm/PyPI/GitHub **[ядро verified в CI]**. **Хвост:** реальный подписанный релиз + полный bundle (GHCR-образ+модель+подписи) на первом `v*`-теге (E2E, maintainer-gated). Owner: **M11.1 + M11.4** |
| GAP-OPS-006 | P3 | **Нет on-disk маркера «ран завершён».** Ни `agentctl`, ни brain не пишут признак завершения: `report.json`/`report.html`/`metrics.prom` создаёт отдельный subcommand `report` (`brain/__main__.py`), поэтому «ран, упавший на 3-м шаге» и «ран в полёте» на диске неотличимы. Следствие: `scripts/collect-live-run.sh` не может отказаться собирать незавершённый ран — он лишь warn'ит об отсутствующих артефактах (осознанно: fail был бы ложноположительным на mid-flight-ране). | Цель: маркер (`status.json` c exit-кодом), пишущийся `agentctl` на выходе; тогда и коллектор, и M15-дашборд смогут отличать crash от in-flight. Owner: **пост-M9-LIVE** |
| GAP-OPS-007 | P2 | **Инвариант группы процессов для cancel держится соглашением, а не тестом на границе.** `POST /v1/runs/{id}/cancel` (ADR-069) сигналит ГРУППЕ, иначе остаются сироты (измерено: 2 из 3 без группы, 0 с группой). Группа создаётся на спавне (`procgroup_unix.go`/`procgroup_windows.go`), но `cancel_test.go` проверяет поведение отмены и НЕ проверяет, что PGID лидера ≠ PGID control-API — то есть регрессия при рефакторинге спавна отправит SIGKILL по группе вызывающего, во всё, что оператор запустил в той же сессии оболочки | `🟢OSS` | тест на границе: PGID лидера ≠ PGID сервиса; `docs/THREAT_MODEL.md` §4.12 |
| GAP-SEC-003 | P2 | **LLM-authoring не умеет `secretRef` → плейнтекст-креды в артефактах.** Authoring-схемы (`brain/planner.py` `_SCHEMA_STEPS`/`_SCHEMA_DRAFT`) содержат только `value`; поля `secretRef` в них нет, поэтому цель вида «залогинься под user/password» материализуется в `plan.json`/`scenario.json` (и в `trace.zip`) **литеральным паролем**. Детерминированный `secretRef`-путь в рантайме существует (`brain/scenario.py`), но LLM в него попасть не может. | **Митигация (landed, M9-LIVE-prep):** `scripts/collect-live-run.sh` по умолчанию обнуляет `value`/`text` у `fill\|type\|select\|press`-шагов без `secretRef`; CI-джоб `collect-live-run-smoke` доказывает это канарейкой. Цель: добавить `secretRef` в authoring-схему + промптовое правило «не инлайнить креды». Owner: **M9-LIVE / M10** |
| GAP-SEC-004 | P3 | **`STORAGE_STATE_SAVE` не защищён от записи внутрь `runs/<id>`.** Путь приходит от вызывающего (`brain/runconfig.py` → `pw-executor/src/server.ts`), кодового барьера «не в артефакт-каталог» нет. Playwright storage-state = auth-cookies + localStorage (материал для session-hijack); оказавшись в `runs/<id>/`, он уехал бы с любым наивным `tar runs/<id>`. | **Митигация (landed, M9-LIVE-prep):** коллектор исключает `*state*.json` безусловно (даже с `--with-trace`) и громко предупреждает о находке. Цель: отклонять `storage_state`-путь внутри артефакт-каталога на уровне кода (по образцу `isUnder`-гарда в `cmd/agentctl`). Owner: **M10** |
| GAP-SEC-005 | P1 | **Нет редактирования на стороне ЗАПИСИ для вывода тестируемого приложения.** С ADR-067 семь кодов `app.*` переносят в `runs/<id>/logs/run.jsonl` то, что напечатало чужое приложение: `console.log` с токеном сессии, URL с `?token=`, текст диалога с ПДн, тело 5xx в сообщении. `logsink.go` кладёт сообщение КАК ПРИШЛО — фильтра по форме секрета (`Bearer `, `eyJ`, `?token=`, длинные hex/base64) на входе нет, в отличие от `brain`, который свои логи редактирует. Защищает только `0700` + токен на чтении; политика хранения и TTL для `logs/` не заданы (у `trace.zip` есть `SENTINEL_TRACE_KEEP`/`SENTINEL_TRACE_TTL_HOURS`, у логов — нет) | `🟢OSS` | write-side redaction + TTL; `docs/THREAT_MODEL.md` §4.12 |
| GAP-UI-001 | P2 | **LLM-подключение из UI не применялось к прогонам.** Хаб `#build` (`docs/index.html`) имел поля LLM (backend/base_url/model/vision), но они уходили только в генератор `sentinel.env`/`run.yaml`; `spawnRun` брал LLM лишь из process env control-API (`cmd.Env = os.Environ()`) — задокументировано как отложенное в ADR-062 (`readyz.go`). Замысел self-contained co-pilot (настройка через UI) не выполнялся. | **CLOSED (ADR-063, 2026-07-23):** `spawnRun` материализует LLM слоями process-env > per-run (`POST /v1/runs` поле `llm`) > persisted (`/v1/config`); хаб `#build`/`#chat` шлют `llm`; секрет-инвариант сохранён (api_key не через UI, noauth-дефолт для local-openai, cloud-ключ в env); per-run `base_url` валидируется (SSRF, общий валидатор с `probeLLM`). Persisted = сервис-тир (профиль `store` в compose). Тесты `llmenv_test.go` + `llm_materialize_test.go`. Смежное: GAP-OPS-005 (конвергенция форм `setup/` vs `#build`). |
| GAP-UI-002 | P2 | **UI не могла получить собственный токен доступа; сплит-режим (`control-api`+`webui` на разных origin) требовал ручной настройки CORS.** До ADR-064 оператор должен был сам придумать `CONTROL_API_TOKEN`, скопировать его в Settings и прописать origin `webui` в `CONTROL_API_CORS_ORIGINS` — иначе браузер блокировал кросс-origin запросы. | **CLOSED (ADR-064, 2026-07-23):** три режима развёртывания (headless / split / single-service) — единственное отличие между ними в том, кто отдаёт браузеру статику UI. Токен больше не нужно придумывать заранее: если `CONTROL_API_TOKEN` не задан, control-api генерирует 32 случайных байта и персистит их в `state/control-api.token` (0600, атомарно), переиспользуя при рестарте; `CONTROL_API_AUTOTOKEN=0` даёт осознанно безтокенный read-only инстанс. Рекомендуемый режим 3 (`CONTROL_API_SERVE_UI=1` + пустой `CONTROL_API_CORS_ORIGINS`) отдаёт UI из того же origin, что и API — allowlist можно оставить пустым, а страница получает адрес и токен через одноразовую ссылку `?bootstrap=<nonce>` (действует 5 минут), напечатанную в логе при старте. **Остающееся ограничение:** bootstrap-nonce доставляется только через лог старта — оператор, потерявший скроллбэк терминала, должен читать токен напрямую из `state/control-api.token`. |

## Продуктовые пробелы (ревизия 2026-07-26 → docs/REGRESSION_MAP.md)

> Найдены при сплошной проверке кода против того, что продукт обещает. Каждый ведёт к пункту `PROD-*`
> в `BACKLOG.md` и к строке в `docs/REGRESSION_MAP.md`. Тир лицензии — по ADR-056 §2.

| ID | Приоритет | Пробел | Тир | Цель / статус |
|----|-----------|--------|-----|----------------|
| GAP-PROD-001 | P1 | **✅ RESOLVED 2026-07-26 (ADR-072, PR #123).** Исполнитель считает сбои приложения (`browser.appFaults`) — считает ЭМИТЕНТ, потому что stderr исполнителя brain не видит; `errors` — гейтимое подмножество, порог `SENTINEL_FAIL_ON_APP_ERRORS`; состояния `pass_with_app_faults`/`problem_app_faults`. Доводка до store и хаба — ADR-076 (PR #125). **События `app.*` не доходят до вердикта.** Семь кодов падений приложения (`js_error`/`console_error`/`console_warn`/`request_failed`/`http_error`/`dialog`/`log_capped`) эмитятся `pw-executor/src/server.ts:79-96`, парсятся `logsink.go` и кладутся в `logs/run.jsonl` — и всё: ни в whitelist артефактов, ни в `resultArtifact`, ни в exit-код. Прогон отдаёт `exit 0` «ПРОЙДЕНО» при сыплющем исключениями приложении | `🟢OSS` | `[PROD-VERDICT-APP]`, свой ADR |
| GAP-PROD-002 | P1 | **✅ RESOLVED 2026-07-26 (ADR-071, PR #122).** Дрейф — первоклассный исход, а не счётчик: re-bind (ключ из плана) отделён от re-ground (выведен с живой страницы), состояния `pass_with_drift`/`problem_drift`, порог `SENTINEL_FAIL_ON_HEAL`. Доводка до store и хаба — ADR-076. **Починка не влияет на вердикт — дрейф UI молчит.** Прогон с N починками даёт `exit 0`; `healed` живёт счётчиком. Граница законного/незаконного внутри `healing.py`: ротация `alternatives[]` пере-привязывает ТОТ ЖЕ элемент (ремонт теста), а `_llm_reground`/`_visual_reground` выбирают НОВЫЙ селектор и могут связаться с другим элементом — проверки «тот ли это элемент» нет | `🟢OSS` | `[PROD-HEAL-VERDICT]`; прецедент — testRigor `fixed-by-ai` diff |
| GAP-PROD-003 | P1 | **Метрика покрытия структурно неверна.** Считается ТОЛЬКО по кнопкам (`graph.py`: `role == "button"`); ссылки/поля/селекты в знаменатель не входят. Хуже: `interactive_seen` накапливается по страницам, поэтому углубление РАСТИТ знаменатель и СНИЖАЕТ покрытие → сходимость `coverage>=0.85 AND фронтир пуст` на многостраничном сайте недостижима, прогон всегда упирается в `max_steps`=40 | `🟢OSS` | `[PROD-CRAWL]`, начинается с ЗАМЕРА на SPA 50+ страниц; поглощает GAP-RISK-003 |
| GAP-PROD-004 | P1 | **Нет дедупликации состояний SPA и возврата между ветками обхода.** Фронтир растёт из `browser.links` того же origin → смена маршрута без `<a href>` невидима; два URL с одинаковым видом считаются дважды, один URL с разными состояниями — один раз | `🟢OSS` | `[PROD-CRAWL]`; `state_hash` необходим, но недостаточен |
| GAP-PROD-005 | P1 | **Версионирования тестов нет.** `plan_hash` — отпечаток целостности, не версия; `TestRecord` — одна строка без ревизий; `baseline` перезаписывает голдены без истории. Переавторинг стирает предыдущий тест бесследно, откатить автономную починку нечем | `🟡SPLIT` | `[PROD-VERSIONING]`: ревизии+откат = Apache, long-retention = 🔒 |
| GAP-PROD-006 | P1 | **Импорта существующих тестов нет.** Ни парсера `.spec.ts`, ни ts-morph/babel — проверено грепом. Экспорт односторонний (`exporter.py` → `@playwright/test`), запись — только собственным MV3-рекордером. Вход только для гринфилда, а платит команда с существующей сюитой | `🟢OSS` | `[PROD-IMPORT]`; импортированный шаг обязан пройти grounding (ADR-022/028) |
| GAP-PROD-007 | P1 | **✅ RESOLVED 2026-07-26 (ADR-073, PR #123).** `brain/junit.py` — чистая детерминированная функция над `heal-report.json`; маппинг наших исходов на четыре формы JUnit и есть дизайн (карантин→skipped, re-ground→system-err vs re-bind→system-out, сбои приложения на сьюте, интегрити→error). ADR-077 добавил `degradations` property. **Нет JUnit XML.** Ни одного репортера (`junit`/`testsuite`/`<testcase`) в `brain/`, `cmd/`, `pw-executor/src/`. Интеграция с CI = «читай exit-код», поэтому GitLab/Jenkins/GitHub/TeamCity не показывают ни шагов, ни времени, ни причины | `🟢OSS` | `[PROD-JUNIT]`, ~150 строк над уже существующим `heal-report.json` |
| GAP-PROD-008 | P2 | **Частоты флейка нет — только бинарный карантин.** «Упал 1 раз из 10» не считается: истории по шагу нет. Бьёт прямо в топ-категорию литературы — Async Wait первый по частоте у Luo et al. (FSE'14) | `🟡SPLIT` | `[PROD-FLAKE-RATE]`: базовые тренды = Apache, ML-скоринг = 🔒 (ADR-056 §2) |
| GAP-PROD-009 | P3 | **Производительность как ось проверок отсутствует.** Измеряется только `duration_ms` на прогон целиком; «уровень производительности» — одна из восьми характеристик ГОСТ Р ИСО/МЭК 25010-2015, и она не закрыта никак | `🟡SPLIT` | `[PROD-PERF]`; инструментарий уже есть (CDP-метрики, сетевой таймлайн в `trace.zip`) |
| GAP-PROD-010 | P3 | **Доступность используется как ХЕШ, а не как критерий.** `_a11y_hash` (`replay.py:41`) ловит «дерево изменилось», но не «стало недоступно»: кнопка без имени, поле без label, контраст, порядок фокуса — мимо. Эталон нормативный: ГОСТ Р 52872-2019 (WCAG 2.1), для госсектора РФ — требование | `🟢OSS` | `[PROD-A11Y]` |
| GAP-PROD-011 | P3 | **Один браузерный движок; кросс-браузерность требует голденов НА ДВИЖОК.** ADR-036 Chromium-only осознанно: `connectOverCDP` только Chromium, и golden-хеши различаются per-engine — эталон из Chromium в Firefox даст `exit 2` на ровном месте | `🟡SPLIT` | `[PROD-ENGINES]`; поглощает GAP-OPS-001 |
| GAP-PROD-012 | P3 | **MV3-рекордер не в CI.** `extension/` собирается только локально (esbuild), живой record→replay ни разу не проверен; при этом это второй из двух входов в продукт | `🟢OSS` | `[M9.8]`; связка с `[PROD-IMPORT]` — оба про вход в существующий проект |
